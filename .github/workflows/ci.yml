name: Secure DevOps CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r app/requirements.txt

      - name: Basic application test
        run: |
          python -m py_compile app/src/app.py

      - name: Security check
        run: |
          pip install bandit
          bandit -r app/src
      - name: Dependency vulnerability scan
        run: |
          pip install pip-audit
          pip-audit -r app/requirements.txt
      - name: Build Docker image
        run: |
          cd app
          docker build -t secure-devops-app:ci .

      - name: Container image security scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: secure-devops-app:ci
          format: table
          exit-code: '1'
          ignore-unfixed: true
          severity: CRITICAL,HIGH
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Docker image
        run: |
          docker tag secure-devops-app:ci ghcr.io/${{ github.repository_owner }}/secure-devops-app:${{ github.sha }}
          docker push ghcr.io/${{ github.repository_owner }}/secure-devops-app:${{ github.sha }}
